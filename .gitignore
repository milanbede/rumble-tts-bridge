# TTS server spool directory — ignore contents but track .gitkeep
tts-server/spool/*
!tts-server/spool/.gitkeep

# Actual user config (config.example.yaml is the template)
config.yaml
**/config.yaml

# Python bytecode
__pycache__/
*.pyc